import { StruCusDirDirective } from './stru-cus-dir.directive';

describe('StruCusDirDirective', () => {
  it('should create an instance', () => {
    const directive = new StruCusDirDirective();
    expect(directive).toBeTruthy();
  });
});
